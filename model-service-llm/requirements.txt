# Qwen 2.5 LLM Service Dependencies
# For agentic OCR reasoning tasks

# RunPod SDK
runpod>=1.6.0

# Model and inference
torch>=2.1.0
transformers>=4.37.0
accelerate>=0.25.0

# 4-bit quantization (optional, will gracefully fallback if unavailable)
bitsandbytes>=0.41.0

# Tokenizer dependencies
sentencepiece>=0.1.99
protobuf>=3.20.0

# Utilities
huggingface_hub>=0.19.0
safetensors>=0.4.0
einops>=0.7.0
