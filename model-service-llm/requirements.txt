# Qwen 2.5 72B LLM Service Dependencies
# For agentic OCR reasoning tasks

# RunPod SDK
runpod>=1.6.0

# Model and inference
torch>=2.1.0
transformers>=4.37.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
sentencepiece>=0.1.99
protobuf>=3.20.0

# For efficient inference (optional but recommended)
# vllm>=0.2.7  # Uncomment if vllm works on your setup

# Auto-GPTQ for quantization (4-bit inference)
auto-gptq>=0.5.0
optimum>=1.14.0

# Utilities
huggingface_hub>=0.19.0
safetensors>=0.4.0
einops>=0.7.0

# JSON parsing
json5>=0.9.0
